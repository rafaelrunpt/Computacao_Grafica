// Mercador agora usa a mesma UI antiga (npc-dialog.js).
// Este ficheiro fica apenas como re-export para manter os imports existentes.
export { abrirDialogoMercador, isDialogoMercadorAberto } from './npc-dialog.js';
